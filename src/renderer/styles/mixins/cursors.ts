import { css } from 'styled-components'

export const pointer = () => css`
    user-select: none;
    cursor: pointer;
`

export const defaultCursor = () => css`
    user-select: none;
    cursor: default;
`
